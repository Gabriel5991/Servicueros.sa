@model IEnumerable<ServicuerosSA.Models.Proveedor_Lote>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Lotes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Proveedores)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Lotes.Codigolote)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Proveedores.Apellidos)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Proveedor_LoteId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Proveedor_LoteId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Proveedor_LoteId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
